# Docker Compose reads this root-level file for variable interpolation.
# Generate two different random values before running docker compose.
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=

# Optional Gemini configuration for the backend container.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash
